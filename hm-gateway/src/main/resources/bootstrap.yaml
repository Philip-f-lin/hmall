spring:
  application:
    name: gateway # 微服務名稱
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.2.132:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml